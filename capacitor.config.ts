import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'G5_HaenggiRouven_Uebungen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
